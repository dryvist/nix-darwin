# Git attributes for nix-darwin repository

# flake.lock - auto-regenerate on merge conflict
# Uses custom merge driver defined in git config (merge.flakelock.driver)
# Driver runs: nix flake lock --no-update-lock-file
flake.lock merge=flakelock
